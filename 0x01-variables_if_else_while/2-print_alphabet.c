#include <stdio.h>
/**
 * main - Print alphabet
 *
 * Return: Always 0 (Success)
 */
int main()
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
          	putchar(a);
	}
        putchar('\n');
        return(0);
}
