#include "main.h"

/**
 * main - Entry point,
 *@ch: character to be checked,
 * print_alphabet: void
 */
void print_alphabet(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar('\n');
}
